import { ImageWrapperComponent } from './blog-image.component';

describe('ImageWrapperComponent', () => {
  it('should create an instance', () => {
    expect(new ImageWrapperComponent()).toBeTruthy();
  });
});
